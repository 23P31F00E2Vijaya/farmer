name: krishi
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_signin_button: ^2.0.0
  cloud_firestore: ^5.5.0
  animated_text_kit: ^4.2.1
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.3
  legacy_progress_dialog: ^0.1.2
  location: ^7.0.1
  geocoding: ^3.0.0  # Updated to geocoding package
  google_sign_in: ^6.2.2
  email_validator: ^3.0.0
  open_mail_app: ^0.4.5
  csc_picker: ^0.2.5
  flutter_neumorphic: ^3.1.0
  carousel_slider: ^5.0.0
  dots_indicator: ^3.0.0
  youtube_player_flutter: ^9.1.1
  http: ^1.2.2
  provider: ^6.0.0
  image_picker: ^1.1.2
  firebase_storage: ^12.3.6
  galleryimage: ^2.0.1
  like_button: ^2.0.3
  flutter_svg: ^2.0.14
  photo_view: ^0.15.0
  google_maps_flutter: ^2.0.10
  map_launcher: ^3.5.0
  url_launcher: ^6.0.12

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
